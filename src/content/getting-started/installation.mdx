---
title: Installation
description: Learn how to install and set up your DocuNext project.
---

## Project Setup

To get started with your own documentation site, clone the repository and install the required dependencies.

```bash
git clone <repository-url>
cd <project-name>
npm install
```

## Running the Development Server

Once the dependencies are installed, you can start the local development server.

```bash
npm run dev
```

Now, open [http://localhost:3000](http://localhost:3000) in your browser to see your documentation site in action. Any changes to the content in the `src/content` directory will be reflected live.
